export const TAP_MOVE_THRESHOLD = 8; // px
export const LONG_PRESS_MS = 600; // ms
export const PIN_HIT_THRESHOLD = 10; // px

export const FIT_PADDING = 50; // px around pins when fitting
export const MAX_FIT_SCALE = 3; // when fitting to all pins
export const MIN_SCALE = 0.1; // global minimum scale
export const MAX_PINCH_SCALE = 6; // maximum scale during pinch zoom
